machine:
  services:
    - docker

checkout:
  post:
    - git submodule sync
    - git submodule update --init # use submodules

test:
  override:
    - sh script/build
    - cp -R tmp/output/ $CIRCLE_ARTIFACTS/
