machine:
  services:
    - docker

checkout:
  post:
    - git submodule sync
    - git submodule update --init # use submodules

dependencies:
  cache_directories:
    - "swagger-codegen"

test:
  override:
    - sh script/build
